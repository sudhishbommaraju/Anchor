# Deploy the static marketing site (repo root) + the console SPA (served at /web/).
# Everything below is backend / tooling and is excluded from the Vercel build,
# so Vercel sees no package.json and serves the site as pure static (no build step).
functions/
web/server.mjs
mcp/
migrations/
scripts/
demo/
node_modules/
.insforge/
.env
.env*.local
package.json
package-lock.json
insforge.toml
README.md
